EXPLAIN `{P9` .撸 '' -- UPDATE IGNORE ( SELECT SQL_CALC_FOUND_ROWS * INTO OUTFILE '' CHARACTER SET `UTF8MB3` FIELDS ESCAPED BY '\1' AS `UTF16` INNER JOIN SELECT * LIMIT @@KV , 0 INTO OUTFILE 'n' CHARACTER SET BINARY LOCK IN SHARE MODE `UTF16` USING ( UNCOMMITTED ) ) SET s . `F` = @'X' := NULL SOUNDS LIKE @'' := `+?` IS NOT FALSE OR `s` ! = `Q` IS TRUE ROLLBACK NO RELEASE ; -- 