self :: text ( ) + / mod / mod / mod / mod - - namespace ( ) / . / ancestor-or-self :: * / parent :: text ( ) * / + / | '}4 ' | / | ancestor-or-self : ancestor ( ) / @ node ( ) / .. | parent :: processing-instruction ( '' ) / namespace :: comment ( ) // . | / | '' div / 